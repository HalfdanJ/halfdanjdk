name: Broken Link Check

on:
  schedule:
    - cron: 0 0 1/5 * *
  repository_dispatch:
  workflow_dispatch:

jobs:
  check:
    name: Broken Link Check
    runs-on: ubuntu-latest
    steps:
      - name: Broken Link Check
        uses: technote-space/broken-link-checker-action@v2
        env:
          TARGET: https://halfdanj.dk
